#include <stdio.h>

int main()
{
    int count = 10;
    float speed = 25.5;
    char grade = 'A';

    count++;
    speed--;

    if (count >= 10 && speed != 0)
    {
        printf("Vehicle speed is high\n");
    }
    else
    {
        count = 0;
    }

    int result;
    result = (count > 10) ? count : 0;

    return 0;
}